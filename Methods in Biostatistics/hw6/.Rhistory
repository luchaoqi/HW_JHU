p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = p1*(1-p2)/((p2)*(1-p2))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p2)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = p1*(1-p2)/((p2)*(1-p2))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = p1*(1-p2)/((p2)*(1-p1))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p1)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p1)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p1)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p1)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p1)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
p1*(1-p2)/((p2)*(1-p1))
p1 = .1; p2 = .1; n1 = 100; n2 = 100
p = log(p1*(1-p2)/((p2)*(1-p1)))
x=rbinom(1000,n1,p1)
y=rbinom(1000,n2,p2)
z=cbind(x,y)
OR = apply(z, 1, function(x) x[1] * (n2 - x[2]) / (x[2] * (n1 - x[1])) )
SELOGOR = apply(z, 1, function(x) sqrt(1 / x[1] + 1 / (n1 - x[1]) + 1 / x[2] + 1 / (n2 - x[2]) ) )
INTERVAL = exp(cbind(log(OR)-1.96*SELOGOR,log(OR)+1.96*SELOGOR))
s=0
for (i in 1:1000) {
if (p>=INTERVAL[i,1] & p<=INTERVAL[i,2]){
s= s+1
}
}
s/1000
